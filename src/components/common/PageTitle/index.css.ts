import { style } from '@vanilla-extract/css'

export const pageTitle = style({
  fontSize: 40,
  marginBottom: 8,
  textAlign: 'center',
})

export const pageInfo = style({
  marginBottom: 16,
  textAlign: 'center',
})
